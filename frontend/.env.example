# 로컬 개발용 — 복사: cp .env.example .env.local
#
# Next.js는 .env.local 을 로컬 전용으로 읽습니다 (.gitignore 대상).
# NEXT_PUBLIC_* 는 브라우저 번들에 포함되므로 비밀값을 넣지 마세요.

# 백엔드 API 베이스 URL (끝에 / 없이)
NEXT_PUBLIC_API_URL=http://localhost:8080
